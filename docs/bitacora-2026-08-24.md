# Bitácora — sesión del 2026-08-24

Continuación de `docs/bitacora-2026-08-23.md`.

Objetivo: **ítem 2 de la sección 6 de `docs/ingestion-architecture.md`** — los
modelos `Source`, `ApiKey` e `IngestionEvent`: schema, migración y tests de
aislamiento. Primera etapa de la capa de ingesta propiamente dicha.

---

## 1. Lo entregado

Rama `feat/ingestion-models`, mergeada tras corrida verde.

- **`prisma/schema.prisma`** — 2 enums nativos (`SourceType`,
  `IngestionStatus`), 3 modelos, relaciones inversas en `Organization` y
  `Contact`. El séptimo `@@unique([organizationId, id])` va en `Source`.
- **`prisma/migrations/20260824120000_ingestion_layer_models`** — escrita a
  mano. `prisma migrate dev` no es una opción: el único `DATABASE_URL`
  disponible apunta al proyecto real de Supabase y ese comando usa shadow
  database. **Quien valida que la migración aplica sobre una base vacía es el
  CI**, que la reconstruye desde cero en cada corrida.
- **`tenant-isolation.integration-test.ts`** — 16 → 24 casos. La suite de
  integración pasa de 56 a **64**.
- **`scripts/verify-schema.ts`** y el `.sql` de diagnóstico — 7 → 8 chequeos
  afirmados.

## 2. Las decisiones que el documento no cubría

El documento de ingesta se escribió antes de la auditoría, así que varias
cosas quedaron sin definir. Se resolvieron **antes** de escribir código, no
durante: la sección de "suposiciones que hubo que hacer" del reporte vino
vacía.

**Soft delete.** Solo `Source` lleva `deletedAt`. `ApiKey` no —precedente
`Invitation`: `revokedAt` ya es el estado terminal y un `deletedAt` encima no
se distingue de él—; `IngestionEvent` tampoco, por ser auditoría append-only
y la tabla de mayor volumen. `Source` sí lo necesita: con
`IngestionEvent → Source` en RESTRICT, una fuente que alguna vez ingestó algo
es indestructible, y sin baja lógica no hay forma de retirarla de la vista.

**`ON DELETE` de las tres FKs compuestas.** `RESTRICT` en las dos que apuntan
a `sources` (columna `NOT NULL`), `NO ACTION` en la de `contacts` (nullable).
La discusión real fue `ApiKey → Source`, donde CASCADE tenía un argumento
genuino —una `ApiKey` es una composición estricta de su `Source`—. Decide la
coherencia: **CASCADE borraría credenciales como efecto colateral
silencioso**, que es exactamente lo que la decisión de `NO ACTION` sobre
`SET NULL` en C-3 ya había rechazado.

**RLS.** Las tres la llevan. `sources` e `ingestion_events` con la política de
aislamiento estándar; **`api_keys` con RLS habilitada y sin ninguna política**
— deny-all para todo rol sin `BYPASSRLS`. Un hash de credencial no debe ser
legible por ningún camino que no sea el backend.

**Alcance de los tests.** Los 16 casos existentes prueban funciones de
repositorio: la propiedad bajo prueba es el `WHERE` que escribe nuestro
código. Los tres modelos nuevos no tienen repositorio todavía, así que un
`prisma.source.updateMany(...)` probaría que Prisma traduce un `WHERE`, no
nuestro contrato. Se escribieron en cambio los **8 casos que prueban lo que la
base rechaza**: 3 de referencia cross-tenant, 3 positivos simétricos, 1 de
`MATCH SIMPLE` con la columna nula, y 2 de idempotencia. Los tests de
escritura van al ítem 3, cuando existan los repositorios.

## 3. Índices: A-6 aplicado desde el nacimiento

Las 6 entidades viejas no tienen índice para su propio patrón de listado. Las
tres nuevas nacen con el suyo, y **sin** el `(organization_id)` suelto que
tienen las viejas: un btree sobre `(organization_id, …)` ya sirve cualquier
`WHERE organization_id = ?`, así que el índice de una columna solo costaría
escrituras y espacio.

El más fino es la cola del worker: `(created_at) WHERE status = 'PENDING'`.
No empieza por `organization_id` porque el worker drena la cola de todas las
organizaciones, y es parcial a propósito — un `(status, created_at)` completo
cargaría cada fila `PROCESSED` para siempre, mientras que el parcial pesa lo
que pesa el backlog y tiende a cero cuando el worker va al día.

Tres ausencias deliberadas, documentadas con su razón: el `(organization_id)`
suelto, `(organization_id, promoted_contact_id)` y
`(organization_id, created_at)` en `ingestion_events`.

## 4. La fila 14 — el punto ciego que se cerró

Se agregó al diagnóstico un chequeo del **conteo de FKs compuestas por
organización**, hoy 18 (15 de `20260821140200` + 3 de la ingesta), afirmado en
`verify-schema.ts`.

Faltaba, y el motivo importa: **la migración de C-3 podía perderse en un
rebase y los 16 tests de aislamiento habrían seguido pasando**, porque prueban
el `WHERE` de la escritura del repositorio, no la constraint. Es el mismo
punto ciego que M-28 y que el fixture de `invitation.service`: la suite
verifica lo que nuestro código escribe, nunca lo que la base rechaza.

Las FKs compuestas son la garantía de aislamiento central del proyecto desde
C-3 y hasta hoy nada comprobaba que existieran.

## 5. El fallo de la primera corrida

La migración aplicó perfecto en el primer intento. Lo que falló fue
`verify:schema`, con un `42601: unterminated quoted string`.

Causa: `extraerConsulta` cortaba el `.sql` en el **primer `;`**, y el texto
nuevo de la fila 5 contenía uno dentro de un literal. El error llegaba tres
pasos después de la causa.

Se corrigió el parser —una máquina de estados que ignora los `;` dentro de
cadenas, con manejo de `''` como escape— en vez de solo sacar el carácter,
porque la alternativa dejaba en pie una regla no escrita ("no pongas un punto
y coma dentro de un literal") en un archivo pensado para pegarse en el SQL
Editor. Mismo criterio con el que se arregló el fixture y no la constraint.

**Hallazgo lateral:** la fila 11 tenía el mismo defecto y no se había
manifestado solo porque la 5 aparece antes. Buscar la clase entera en vez de
la instancia que falló evitó que reapareciera en el próximo cambio.

## 6. Resultado

Corrida verde sobre `b78e5d2`:

- Las **9 migraciones** aplican sobre una base vacía; SQL manual y seed OK.
- `verify:schema`: **8 chequeos afirmados** — 12 políticas RLS, 8 índices
  únicos parciales, **18 FKs compuestas**, más los de C-1 y C-2.
- **64 de 64** tests de integración, con los 8 nuevos verificados por nombre.

## 7. Añadido al documento de ingesta

Sección 9, "Notas de implementación". Anota, no modifica las secciones 1 a 8:

- **§9.1 — Particionar `IngestionEvent` por fecha es incompatible con la
  idempotencia.** Postgres exige que toda constraint única de una tabla
  particionada incluya la clave de partición, así que el único parcial pasaría
  a `(sourceId, externalId, createdAt)`, que no garantiza nada: el mismo
  `externalId` reingresado al día siguiente cae en otra partición y entra
  limpio. La purga razonable no necesita ninguna columna nueva.
- **§9.2 — Falta un identificador de lote.** La sección 5 exige reportar
  "cuántos entraron, cuántos se promovieron, cuántos fallaron", pero un
  `IngestionEvent` conoce su `Source`, no la importación que lo trajo. Queda
  como requisito abierto del ítem 5. Agregarlo después es barato: `ALTER TABLE
  ADD COLUMN` de una columna nullable sin default no reescribe la tabla.

## 8. Lo que sigue

**Ítem 3: gestión de API keys** (crear, listar, revocar) por el camino de auth
existente, con rol `ADMIN`. Ahí entran los repositorios y los services de los
tres modelos, y con ellos los tests de escritura que quedaron diferidos.

Dos cosas a resolver en el camino:

- **M-13, antes del ítem 4.** La sección 4 apoya toda la deduplicación en un
  "upsert por email" contra el único parcial `(organization_id, email)`, pero
  ese índice es case-sensitive y la normalización vive solo en
  `contact.service.ts`, que la promoción masiva no ejecuta. Índice sobre
  `lower(email)` o `citext`.
- **El algoritmo de hasheo de la API key**, que el ítem 2 dejó explícitamente
  sin decidir: la columna es `text` sin presuponer largo, y `keyHash` tiene
  `@unique` global porque `authenticateApiKey` busca sin conocer todavía la
  organización.

**Registrados sin corregir:** A-6 para las 6 entidades viejas, y los mismos
MEDIO/BAJO de la sesión anterior.

---

# Ítem 3 — gestión de API keys (mismo día, sesión siguiente)

Rama `feat/api-key-management`. Corte vertical completo: repository → service
→ controller → routes, para `Source` y `ApiKey`. **Sin migración**: los
modelos existen desde el ítem 2.

## 9. El alcance tuvo que ampliarse

El ítem 3 del documento, tal como está escrito, **no se puede usar**: dice
"gestión de API keys" pero una `ApiKey` exige un `sourceId`, y ningún ítem
crea `Source`. El endpoint de creación no tendría a qué apuntar.

Se incluyó el CRUD de `Source` con cinco operaciones —crear, listar, ver,
pausar/reactivar y retirar—, todas con rol `ADMIN`, incluidas las lecturas:
una `Source` es configuración de integración, no un módulo de negocio de
lectura abierta como `Company` o `Contact`. Precedente: `user.routes.ts` e
`invitation.routes.ts`.

`fieldMapping` quedó **afuera**, ni en create ni en update. El documento dice
"JSONB" y nada más — ni forma, ni quién la consume. Definirla ahora sería
inventar una estructura de datos, que es exactamente lo que la regla del
proyecto prohíbe. La define el ítem 4, cuando tenga un payload real enfrente.
Tampoco se puede cambiar `type` en el PATCH: obligaría a decidir qué pasa con
los `IngestionEvent` ya ingeridos bajo el tipo anterior, y no hay respuesta
buena.

## 10. Las decisiones de seguridad

**El hasheo**, que el ítem 2 había dejado explícitamente abierto: SHA-256
determinístico y sin sal, con `randomBytes(32)` como requisito enunciado, no
como detalle. El razonamiento completo quedó en
`docs/ingestion-architecture.md` §9.3, porque es una decisión de seguridad que
tiene que ser encontrable sin leer una bitácora.

**Revocar dos veces da 409**, con compare-and-swap, calcado de
`revokeInvitationConditional` — incluida la disciplina de que `count === 0` es
la única fuente de verdad y la relectura posterior solo explica, nunca decide.
`DELETE` deja de ser idempotente, igual que en `Invitation`: tener dos
convenciones distintas para la misma semántica es peor que la desviación.

**`x-api-key` entró a `REDACT_PATHS`** ahora, aunque el header no se use hasta
el ítem 4. Es defensa de logging, no autenticación, y el costo de olvidarla
sería una credencial viva en un log.

**La verificación del logging se hizo contra los serializers instalados**
(`pino-std-serializers@7.1.0`), no de memoria: `lib/req.js` serializa `url`,
`query` y `params`; ninguno toca un body. Conclusión: hoy la clave en claro no
puede aparecer en un log, porque solo existe en el cuerpo de una respuesta.
Pero de ahí sale una restricción dura para el ítem 4, que quedó como **control
negativo en `logger.test.ts`** en vez de como comentario:

> La clave viaja **solo** por el header `X-API-Key`, nunca por la URL.
> Aceptarla por querystring sería un leak inmediato y silencioso: `redact` no
> la alcanza ahí.

## 11. La cascada — decisión tomada por Claude Code, aprobada

`DELETE /api/sources/:id` revoca todas las claves de la fuente en la misma
transacción. No estaba pedida. Se aprobó porque mueve la garantía de un
chequeo que alguien tiene que acordarse de escribir a un hecho en los datos —
el mismo argumento de C-3. Documentada en §9.4 del documento de ingesta, con
el detalle de que filtra `revokedAt: null` y por lo tanto **no pisa la fecha
real** de las claves ya revocadas (la clase de defecto que la auditoría
registra como M-1).

## 12. Una divergencia deliberada que no hay que armonizar hacia atrás

Los dos repositorios nuevos usan un `PUBLIC_SELECT` con la lista explícita de
columnas, en vez del `findMany` sin `select` que usan los 8 módulos
existentes y que devuelve la fila cruda de Prisma con `deletedAt` y
`organizationId` incluidos (hallazgo BAJO de la auditoría).

En `api_keys` no es cosmético: un `findMany` sin `select` devuelve `keyHash`,
y de ahí a un `res.json()` hay un paso. Con la proyección en un solo lugar,
exponer material criptográfico exige editar esa constante a propósito.

Está marcado como divergencia en el código, con la instrucción de no
armonizarla. Mismo criterio con el que los índices de estas tablas nacieron
bien mientras las viejas siguen sin los suyos.

## 13. Columnas sin escribir, y quién las va a escribir

"Todavía no" y "nunca" se ven iguales en una base de datos. La diferencia con
M-9 —`lastLoginAt`, que nunca tuvo escritor planeado y el frontend muestra
siempre vacía— es que estas tres tienen ítem asignado:

| Columna | Estado | Quién la escribe |
|---|---|---|
| `ApiKey.lastUsedAt` | Siempre `null`, expuesta en la API | Ítem 4, al resolver una clave |
| `Source.fieldMapping` | Siempre `null`, **no expuesta** | Ítem 4, con un payload real |
| `IngestionEvent.*` | La tabla entera sigue vacía | Ítems 4 y 5 |

`IngestionEvent` tampoco tiene tests de escritura todavía, y la razón cambió:
en el ítem 2 era que no tenía repositorio; ahora es que **nada lo escribe**,
así que darle uno sería crear código muerto para poder testearlo. Sus
escrituras nacen con el worker.

## 14. Resultado

Corrida verde:

- **79 tests de integración** (64 → 79): los 4 diferidos del ítem 2 sobre las
  escrituras nuevas —incluida `revokeApiKeysBySource`, la escritura masiva, la
  que más caro sale si el aislamiento falla— más 11 de endpoint sobre HTTP
  real con los routers reales.
- **66 unitarios** (56 → 66), con los del generador y el control negativo del
  log. Uno de ellos prueba que `hashApiKey` **no normaliza**: la clave, la
  clave con un espacio, con mayúsculas y con un salto de línea dan cuatro
  hashes distintos. El día que alguien agregue un `.trim()` "por robustez", el
  test lo frena.
- `verify:schema` con sus 8 chequeos: esta etapa no toca el esquema.

## 15. Lo que sigue

**Ítem 4: webhook de landing page** — `authenticateApiKey`, `IngestContext`,
rate limit por clave, y el ciclo completo staging → promoción. Es el primero
que escribe `IngestionEvent`.

Con dos cosas pendientes que son prerequisito suyo:

1. **M-13.** La sección 4 apoya toda la deduplicación en un "upsert por email"
   contra el único parcial `(organization_id, email)`, que es case-sensitive y
   cuya normalización vive solo en `contact.service.ts` — que la promoción
   masiva no ejecuta.
2. **La restricción del header**, de §10: la clave nunca por la URL.
